{
  "name": "pedigree-monorepo",
  "private": true,
  "version": "0.1.0",
  "description": "Pedigree — the human org chart for AI agents.",
  "workspaces": [
    "apps/web"
  ],
  "scripts": {
    "dev:web": "npm --workspace apps/web run dev",
    "build:web": "npm --workspace apps/web run build",
    "preview:web": "npm --workspace apps/web run preview"
  },
  "engines": {
    "node": ">=20"
  }
}
